# Supabase

`schema.sql` (users/projects/tasks/activity_log tables + RLS policies per the
data model in the WorkSync build brief) is built in **Phase 1**. This
directory is scaffolded now so `scripts/seed.js` has a documented target to
run against once that lands.
